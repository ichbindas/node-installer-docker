# Dusk Node Installer Configuration
# Copy this file to dusk.env and modify as needed
# The installer will automatically load this file if it exists

# Note: Binary is always downloaded from latest official release
# See: https://github.com/dusk-network/rusk/releases

# Number of nodes to install/add (default: 1)
ADD_NODES=2

# Enable fast-sync during installation (default: false)
FAST_SYNC_ENABLED=false

# Base directory for Dusk installation (default: /opt/dusk)
# Alternatives: /usr/local/dusk, /var/lib/dusk
DUSK_BASE_DIR=/opt/dusk

# Base port for RPC (default: 18080)
# Each node will use the next available port (18080, 18081, 18082, ...)
BASE_RPC_PORT=18080

# Base port for P2P (default: 9000)
# Each node will use the next available port (19000, 19001, 19002, ...)
BASE_P2P_PORT=19000

# RPC address binding (default: 0.0.0.0)
# Use 0.0.0.0 to allow external connections, 127.0.0.1 for localhost only
RPC_ADDRESS=0.0.0.0

# Network configuration (mainnet or testnet)
NETWORK=mainnet
#NETWORK=testnet

# Bootstrap nodes are automatically configured from official Dusk sources:
# Mainnet: 165.232.91.113:9000, 64.226.105.70:9000, 137.184.232.115:9000
# Testnet: 173.212.198.91:9000, 188.34.201.78:9000, 94.72.105.11:9000, 64.44.41.61:9000, 158.220.83.234:9000
# Source: https://docs.dusk.network/operator/from-source/
# No manual configuration required

# Maximum number of peers to connect to (default: 50)
MAX_PEERS=50

# P2P connection timeout in seconds (default: 30)
P2P_CONNECTION_TIMEOUT=30

# P2P handshake timeout in seconds (default: 10)
P2P_HANDSHAKE_TIMEOUT=10

# Storage limit per node (default: 50GB)
# Examples: "50GB", "100GB", "500GB"
STORAGE_LIMIT=50GB

# CORS origins for RPC endpoint (comma-separated)
# Default allows localhost and localhost with wildcard port
# Examples: "http://localhost:*,https://localhost:*"
# Production: "https://your-domain.com"
CORS_ORIGINS=http://localhost:*,https://localhost:*

# Docker user configuration
# Leave empty to use current user, or specify a user/group
# Format: USER:GROUP or USER (will use same name for group)
# Example: dusk:dusk or 1000:1000
DOCKER_USER=

# Flask API configuration
FLASK_API_ENABLED=true
FLASK_API_PORT=5000

# Logging configuration
LOG_LEVEL=info
