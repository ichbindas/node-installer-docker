# Dusk Node Installer Configuration
# Copy this file to dusk.env and modify as needed
# The installer will automatically load this file if it exists

# Note: Binary is always downloaded from latest official release
# See: https://github.com/dusk-network/rusk/releases

# Number of nodes to install/add (default: 1)
ADD_NODES=1

# Enable fast-sync during installation (default: false)
FAST_SYNC_ENABLED=false

# Base directory for Dusk installation (default: /opt/dusk)
DUSK_BASE_DIR=/opt/dusk

# Network configuration (mainnet or testnet)
NETWORK=mainnet